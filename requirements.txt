# terrariabonker runtime dependencies.
#
# Install with pip:            pip install -r requirements.txt
# Or via distro packages (Arch/CachyOS, recommended — system python):
#     sudo pacman -S python-numpy python-pillow
#
# numpy  — fast player-block scanning in the memory locator
# Pillow — decode the game's Item_<id>.xnb sprites into the local icon cache
#          (the recipe browser rebuilds this cache from your own game files;
#           no sprites are shipped)
#
# The control panel is a Go program now (terrariabonker-gui, spec 050), so PyQt6
# has left. numpy and Pillow leave with the rest of the Python (spec 051).
numpy
Pillow
