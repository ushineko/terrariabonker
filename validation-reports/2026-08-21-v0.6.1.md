# Validation Report — terrariabonker v0.6.1

**Date**: 2026-08-21
**Scope**: Canonical rarity names in the slot tooltip (follow-up to spec 007).

## Summary

The inventory tooltip now shows the rarity tier's canonical Terraria name alongside
the number — "Rarity: Yellow (8)" instead of "Rarity 8". A `RARITY_NAME` map (the
`ItemRarityID` names: Gray/White/Blue/Green/Orange/Light Red/Pink/Light Purple/Lime/
Yellow/Cyan/Red/Purple, plus Quest/Expert/Master) sits next to the colour map in the
Qt-free `gui/invgrid.py`; an unknown tier falls back to the bare number.

## Phase 3 — Tests

- **71 passed** headless (1 new: `rarity_name` for known tiers and unknown fallback;
  the tooltip test now asserts the named form). Verified across tiers: White (0),
  Blue (1), Green (2), Yellow (8), Red (10), Gray (−1), Quest (−11).

## Phase 4 — Code quality

- `ruff check` → passed. Name map is data next to `RARITY_RGB`; `rarity_name` mirrors
  `rarity_rgb`. No behaviour beyond the tooltip string changed.

## Phase 5 — Security review

- No new inputs, network, privilege, or dependencies. Cosmetic string change. Secrets:
  none.

## Phase 5.5 — Release safety

- **Rollback**: `git revert`. Additive display-only change.

## Result

All gates pass. Approved for commit and tag `terrariabonker/v0.6.1`.
